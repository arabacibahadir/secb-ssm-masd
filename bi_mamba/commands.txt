# Bi-Mamba-S commands

## Prepare windowed EEG data
python bi_mamba/train.py --prepare --label-source state --focused-minutes 10 --unfocused-minutes 20 --mat-root EEGData --out-dir data/eeg_attention

## Train and evaluate
python bi_mamba/train.py --out-dir data/eeg_attention --epochs 50 --batch-size 64 --use-amp --no-normalize
